importar opencvjp

modelo = cv.modelo_haarcascade("bibliotecas/opencvjp/data/haarcascade_frontalface_default.xml")
cam = cv.camera(0)

loop:
    frame = cv.obter(cam)
    total = cv.detectar(modelo, frame)
    cv.exibir("Camera", frame)
    
    se cv.tecla(27):
        parar

cv.fechar()