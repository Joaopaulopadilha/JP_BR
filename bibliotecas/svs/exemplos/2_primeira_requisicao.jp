importar svs

porta = svs_criar(8080)
ip = svs_meu_ip()

saida("servindo em: {ip}:{porta}")

enquanto verdadeiro:
    req = svs_aguardar(porta)
    saida("=== Requisição recebida ===")
    saida("Método: " + svs_metodo(req))
    saida("Caminho: " + svs_caminho(req))
    svs.fechar(req)